# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES.
#                         All rights reserved.
# SPDX-License-Identifier: Apache-2.0
---
{% set name = "realm-gex-wrapper" %}

{% set legate_version = environ.get('LEGATE_VERSION') %}

{% set legion_sha = environ.get('LEGION_GIT_REV', 'master') %}
{% set git_url = environ.get('LEGION_GIT_URL', 'https://gitlab.com/StanfordLegion/legion.git') %}

package:
  name: {{ name|lower }}
  version: {{ legate_version }}

source:
  git_url: {{ git_url }}
  git_rev: {{ legion_sha }}
  patches:
    - patches/realm-gex-wrapper-link-gasnet.patch

build:
  include_recipe: false
  # Compatibility is tied to the exact Legate version below. Bump this only
  # when rebuilding the same version with changed wrapper packaging.
  number: 0
  skip: true  # [not linux]
  noarch: generic
  script_env:
    - PKG_NAME={{ name }}

requirements:
  run:
    - legate ={{ legate_version }}

extra:
  recipe-maintainers:
    - m3vaz
    - marcinz
