#!/usr/bin/env bash


build_all() {
    set -x
    cd ~/;

    conda info

    set -euo pipefail;

    build-legate-cpp;
    build-legate-wheel;
    build-legate-conda;
    build-cpp-example;
    build-cpp-test;
}

(build_all "$@");
