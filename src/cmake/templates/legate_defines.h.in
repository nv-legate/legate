// -*- mode: fundamental-mode  -*-
/*
 * SPDX-FileCopyrightText: Copyright (c) 2024 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: LicenseRef-NvidiaProprietary
 *
 * NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
 * property and proprietary rights in and to this material, related
 * documentation and any modifications thereto. Any use, reproduction,
 * disclosure or distribution of this material and related documentation
 * without an express license agreement from NVIDIA CORPORATION or
 * its affiliates is strictly prohibited.
 */

#pragma once

#include <legion.h>

#include <legate/version.h>

#ifndef LEGION_REDOP_HALF
#error "Legate needs Legion to be compiled with -DLEGION_REDOP_HALF"
#endif

#cmakedefine LEGATE_USE_DEBUG @LEGATE_USE_DEBUG@

#cmakedefine LEGATE_USE_CUDA @LEGATE_USE_CUDA@

#cmakedefine LEGATE_USE_NETWORK @LEGATE_USE_NETWORK@

#cmakedefine LEGATE_USE_OPENMP @LEGATE_USE_OPENMP@

#cmakedefine LEGATE_USE_CAL @LEGATE_USE_CAL@

#define LEGATE_SHARED_LIBRARY_SUFFIX "@CMAKE_SHARED_LIBRARY_SUFFIX@"

#define LEGATE_SHARED_LIBRARY_PREFIX "@CMAKE_SHARED_LIBRARY_PREFIX@"

#define LEGATE_CONFIGURE_OPTIONS "@LEGATE_CONFIGURE_OPTIONS@"

#if !defined(LEGATE_USE_CUDA) && defined(LEGION_USE_CUDA)
#define LEGATE_USE_CUDA 1
#endif

#if !defined(LEGATE_USE_OPENMP) && defined(REALM_USE_OPENMP)
#define LEGATE_USE_OPENMP 1
#endif

#ifndef LEGATE_USE_NETWORK
#if defined(REALM_USE_GASNET1) || defined(REALM_USE_GASNETEX) || defined(REALM_USE_MPI) || \
  defined(REALM_USE_UCX)
#define LEGATE_USE_NETWORK 1
#endif
#endif

#ifdef LEGION_BOUNDS_CHECKS
#define LEGATE_BOUNDS_CHECKS 1
#endif

#define LEGATE_MAX_DIM LEGION_MAX_DIM

// TODO(wonchanl): 2022-10-04: Work around a Legion bug, by not instantiating futures on
// framebuffer.
#define LEGATE_NO_FUTURES_ON_FB 1

namespace legate {

extern const char *const LEGATE_GIT_HASH;
extern const char *const LEGION_GIT_HASH;

} // namespace legate
