# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES.
#                         All rights reserved.
# SPDX-License-Identifier: Apache-2.0
---
on:
  workflow_call:
    inputs:
      platform:
        type: string
        required: true
      ref-sha:
        required: false
        type: string
        default: ''
      target-device:
        type: string
        required: true
      build-mode:
        type: string
        required: true
      upload-docs-to-gh-pages:
        type: boolean
        required: false
        default: false
      cuda-version:
        type: string
        required: false
      ci-verbose:
        required: false
        type: boolean
        default: false
    secrets:
      NV_LEGATE_INTER_REPOS_ACCESS:
        required: true

jobs:
  build-docs:
    if: ${{ github.repository_owner == 'nv-legate' }}
    uses:
      nv-legate/legate-gh-ci/.github/workflows/gh-build.yml@v1.50
    with:
      build-has-tests: false
      client-repo: ${{ github.event.repository.name }}
      client-ref: ${{ inputs.ref-sha }}
      target-device: ${{ inputs.target-device }}
      runs-on: ${{ (inputs.platform == 'linux' && 'linux-amd64-cpu16') || (inputs.platform == 'mac' && 'macos-latest') }}
      build-type: docs
      use-container: ${{ inputs.platform == 'linux' }}
      platform: ${{ inputs.platform }}
      legate-gh-ci-tag: "v1.50"
      build-mode: ${{ inputs.build-mode }}
      network: "ucx"
      python-version: "3.14"
      setup-python-proxy-cache: true
      timeout: 60
      cuda-version: ${{ inputs.cuda-version }}
      docker-image: "condaforge/miniforge3:25.3.1-0"
      ci-verbose: ${{ inputs.ci-verbose }}
    secrets: inherit

  upload-docs-to-gh-pages:
    if: ${{ inputs.upload-docs-to-gh-pages && github.ref_name == 'main' }}
    needs: build-docs
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Dump build-docs output variables
        shell: bash --noprofile --norc -euo pipefail {0}
        run: |
          echo "${{ needs.build-docs.outputs.artifact-name }}"
          echo "${{ needs.build-docs.outputs.artifacts-dir }}"

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.build-docs.outputs.artifact-name }}
          path: ${{ needs.build-docs.outputs.artifacts-dir }}

      - name: Summarize downloaded artifacts
        shell: bash --noprofile --norc -euo pipefail {0}
        run: |
          pwd
          files=$(mktemp)
          find "${{ needs.build-docs.outputs.artifacts-dir }}" -type f | sort > "${files}"
          file_count=$(wc -l < "${files}" | tr -d '[:space:]')
          echo "Files: ${file_count}"
          # This upload job does not check out legate-gh-ci, so use the
          # workflow input directly instead of LEGATE_CI_VERBOSE.
          if [[ "${{ inputs.ci-verbose }}" == "true" ]]; then
            cat "${files}"
          else
            sed -n '1,200p' "${files}"
            if (( file_count > 200 )); then
              echo "... $((file_count - 200)) more files hidden (run with ci-verbose=true for full list)"
            fi
          fi
          rm -f "${files}"

      - name: Find index.html's parent folder
        shell: bash --noprofile --norc -euo pipefail {0}
        id: find_docs_dir
        run: |
          FILE_PATH="$(
            find "${{ needs.build-docs.outputs.artifacts-dir }}" -name "index.html" -printf '%d %p\n' \
              | sort -nk1 \
              | cut -d' ' -f2- \
              | head -n 1
          )"
          if [[ -z "${FILE_PATH}" ]]; then
            echo "index.html not found" >&2
            exit 1
          fi
          PARENT_DIR=$(dirname "${FILE_PATH}")
          echo "docs_dir=${PARENT_DIR}" >> "${GITHUB_OUTPUT}"

      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: ${{ steps.find_docs_dir.outputs.docs_dir }}
          token: ${{ secrets.NV_LEGATE_INTER_REPOS_ACCESS }}
          repository-name: "nv-legate/legate"
